# Ablation Study: 극한 증강(Extreme Augmentation) 도입 근거 및 실험 설계

## 1. 개요
본 프로젝트의 최종 목적은 '라벨링 된 최신/깔끔한 도면'으로 학습한 모델을 **'열화된 구형 아파트 스캔 도면(90년대)'**에 적용하는 것(Domain Adaptation)임. 
이를 위해 실제 과거 도면의 물리적 열화 환경을 수학적 증강 파라미터로 매핑하여 학습에 강제로 주입함.

## 2. 증강 파라미터 선정 근거 (논문/연구 기반 휴리스틱)

### A. 삐뚤어짐 및 회전 (degrees: 5.0)
- **원인**: 과거 종이 도면을 평판 스캐너나 복사기로 복제할 때 발생하는 물리적 스큐(Skew) 현상.
- **학술 근거**: 일반적인 문서 스캔 열화 및 OCR 인식 관련 연구(Document Image Skew Detection)에 따르면, 수작업 스캔 시 발생하는 최대 오차 허용 범위는 통상 ±3~5도 내외임. 
- **효과 분석**: 도면의 수평/수직 구조선에만 모델이 과적합(Overfitting)되는 것을 방지하고, 비뚤어진 실전 도면에서도 공간 구획을 인식할 수 있는 회전 불변성(Rotation Invariance) 성능 기여도 추적.

### B. 노출 불량 및 명암비 저하 (hsv_v: 0.5, hsv_s: 0.0)
- **원인**: 잉크 번짐, 종이 변색(황변 현상), 복사기 토너 부족, 스캐너 광원 불균일.
- **학술 근거**: 고문서 및 레거시 문서 이진화(Legacy Document Image Binarization) 연구에서 옛날 문서는 조명 텍스처 불균일과 저대비(Low Contrast)가 가장 큰 노이즈 원인으로 지목됨. 채도(Saturation)를 0으로 만들어 흑백에 맞추고, 명도(Value) 범위를 강제로 흔들어 노출 불량을 시뮬레이션.
- **효과 분석**: 객체 탐지 시 색상(Color) 피처에 대한 의존도를 완전히 없애고, 오직 '선과 형태(Edge & Shape)'만으로 가구/텍스트를 분류하도록 강제하여 성능 향상 기여.

### C. 초점 흐림 및 화질 저하 (blur: 0.25)
- **원인**: 저해상도 팩스 전송, 다중 복사(Copy of copy)로 인한 윤곽선 열화 및 뭉개짐.
- **학술 근거**: Gaussian Blur 적용 시 고주파(High-frequency) 디테일이 소실되며, 이는 실제 저해상도/저품질 스캔 문서의 물리적 노이즈 분포와 수학적으로 유사함.
- **효과 분석**: 깔끔한 CAD 도면에서만 보이는 칼같은 테두리(Edge) 픽셀에 모델이 민감하게 반응하는 것을 억제. 뭉개진 형태에서도 특징을 추출하는 강건성(Robustness) 향상.

## 3. 실험 및 검증 방법 (비교 및 반복 프로세스)

단순히 증강을 때려 넣고 끝내는 것이 아니라, 해당 원인들이 실제 성능 향상에 기여했음을 증명하기 위한 반복 실험(Ablation Study) 루프를 구성함.

1. **대조군(Baseline) 모델 확보**: 증강 전면 비활성화 (`degrees=0`, `blur=0`, `hsv=0`) 상태로 학습.
2. **실험군(Extreme Aug) 모델 학습**: 상기 기재된 파라미터 적용하여 학습.
3. **정량적 평가 (Validation)**: 깨끗한 Val Set에서의 mAP 수치 변동 폭을 비교 (과도한 증강으로 인한 Underfitting 여부 체크).
4. **정성적 평가 및 원인 분석 (Real-world Testing)**: 
   - 라벨이 없는 '실제 노출 불량/삐뚤어진 구형 도면' 이미지를 두 모델에 동시 추론(Inference)시킴.
   - 대조군 모델이 탐지에 실패(False Negative)한 객체를 실험군 모델이 어느 정도 잡아내는지 시각적으로 교차 검증.
   - *ex) "5도의 Skew 증강을 추가했더니, 실제로 우측으로 살짝 기울어진 90년대 도면에서 싱크대 인식률이 체감상 대폭 상승함을 확인"*
5. **피드백 및 파라미터 미세 조정 (Iteration)**: 
   - 증강 강도가 너무 세서 원본의 필수 특징까지 파괴된다면(예: 문자를 선으로 오인), blur 수치를 낮추거나 degrees를 2.0으로 낮추는 등 논리적 원인에 기반하여 파라미터 조정 반복 수행.

## 4. 궁극적 도면 디지털화(JSON)를 위한 마스터 파이프라인 아키텍처

본 프로젝트는 단순 BBox 탐지를 넘어 프롭테크 등에 즉시 활용 가능한 '완벽한 3D 건축 데이터 변환 솔루션' 구축을 궁극적 목표로 삼고 있으며, 이를 달성하기 위한 단계별 아키텍처는 다음과 같음.

### 4.1 현재의 해결책: 위상학적 후처리 (Topological Post-processing)
- 가구와 텍스트를 단순 BBox로 추출할 시 발생하는 좌표의 비뚤어짐과 부정확성 한계를 극복하기 위해 알고리즘 기반 후처리 전격 도입.
- **직교화(Orthogonalize)**: 약간 비스듬하게 쳐진 박스의 각도를 완벽한 수평/수직(90도)으로 강제 교정.
- **스냅(Snap)**: 근접한 가구/객체들의 선이 미세하게 어긋난 경우, 일직선 상에 맞추어 통일시킴으로써 무거운 Segmentation 모델 없이도 CAD 데이터 수준의 정제된 고품질 JSON 결과물 산출.

### 4.2 미래 확장 파이프라인 (Future Work)
현재의 컴퓨팅 자원 및 단기 프로젝트 기한으로 인해 즉각 도입은 어려우나, 실전 레거시 도면의 완벽한 구조화를 위해 반드시 결합되어야 할 4대 핵심 기술 방향성.

1. **생성형 AI 전처리 (Generative Restoration)**: 
   - **모델**: Diffusion, Pix2Pix 등
   - **역할**: 기존 필터(OpenCV)의 한계를 넘어, 낡고 훼손된 도면의 희미해진 벽체 선과 기호를 생성형 AI가 새것처럼 뚜렷하게 복원(Redraw).
2. **End-to-End 벡터화 (Space/Structural Segmentation)**: 
   - **모델**: RoomFormer, FloorplanVLM 등 (트랜스포머 기반 비전 모델)
   - **역할**: YOLO의 BBox 탐지를 넘어 벽체 두께, 창문, 문, 방(Space)의 전체 윤곽선을 한 번에 폴리곤(Polygon) 벡터 데이터로 통째로 추출. 데이터 구성 난이도 및 컴퓨팅 자원 소모가 커 현재는 보류됨.
3. **위상학적 일관성 검증 (Graph Neural Networks, GNN)**: 
   - **구조**: 방을 노드(Node), 문/연결성을 엣지(Edge)로 구성.
   - **역할**: "문이 허공에 떠있거나 벽이 막혀있지 않은" 논리적 오류 방지. 건축학적 상식(Topology)을 모델이 이해하고 스스로 분할 결과를 교정하도록 수학적으로 강제.
4. **특수 기호 극복을 위한 Few-shot 파인튜닝**: 
   - **목적**: 시대 변화에 따라 아예 다르게 그려지는 건축 기호(예: 80년대와 2020년의 욕조 심볼 차이)는 노이즈 증강만으로 유추가 불가능(논리적 단절)하므로, 소량의 진짜 레거시 도면을 라벨링하여 모델을 미세조정(Fine-tuning)함으로써 상용화율 100% 달성.
